#include <GL/gl.h>
#include <GL/glut.h>
#include <time.h>
#include <math.h>

// Global variables for window size, etc.
// Functions for drawing the clock face, hands, and updating time.

void display() {
    // Clear buffer, set up projection
    // Draw clock face
    // Get current time
    // Calculate hand angles and draw hands
    // Swap buffers
}

void timer(int value) {
    // Request redisplay
    // Set up next timer call
}

int main(int argc, char** argv) {
    // Initialize GLUT, create window
    // Register display and timer callbacks
    // Enter main loop
    return 0;
}