int myArray[5]; // Declares an array named myArray that can hold 5 integers.
int initializedArray[3] = {10, 20, 30}; // Declares and initializes with specific values.

int inferredSizeArray[] = {5, 4, 3, 2, 1}; // Compiler determines the size (5 in this case).

int myArray[5] = {1, 2, 3, 4, 5};
int thirdElement = myArray[2]; // Accesses the third element (value 3)
myArray[0] = 100; // Modifies the first element