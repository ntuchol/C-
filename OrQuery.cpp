if (age < 18 || hasPermission) {
        // Code to execute if age is less than 18 OR hasPermission is true
    }
    int a = 5;  // Binary: 0101
    int b = 3;  // Binary: 0011
    int result = a | b; // Binary: 0111 (Decimal: 7)
    if (isLoggedIn or isAdmin) {
        // Code to execute if isLoggedIn is true OR isAdmin is true
    }