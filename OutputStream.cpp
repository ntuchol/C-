#include <iostream> // Required for cout and output streams

int main() {
    // Variable initialization
    int myVariable = 42; 

    // Writing the variable's value to the standard output stream (cout)
    std::cout << "The value of myVariable is: " << myVariable << std::endl;

    return 0;
}