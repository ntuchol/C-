
// Example: Convert an annual rate (e.g., 12%) to a monthly rate
double annualRate = 0.12; // Represent as a decimal
double monthlyRate = annualRate / 12.0;
monthly = (L * pow(monthlyrate + 1, n) * monthlyrate) / (pow(monthlyrate + 1, n) - 1);
