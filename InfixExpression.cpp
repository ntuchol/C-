// Infix expressions in C++
int result1 = 5 + 3 * 2; // Multiplication (3*2) happens before addition, result is 11
int result2 = (5 + 3) * 2; // Parentheses force addition (5+3) first, result is 16
bool condition = (x > 5) && (y < 10); // Logical AND operator between two boolean expressions