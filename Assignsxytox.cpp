if (y != 0) {
    x = x / y;
} else {
    // Handle the case where y is 0 (e.g., print an error, assign a default value, etc.)
    // For example:
    // std::cerr << "Error: Division by zero!" << std::endl;
    // x = 0; // Assign a default value
}