#include <iostream>
#include <string>
#include <iomanip> 

int main() {
    
    if (isWeekday && startTime >= 8.00 && startTime <= 18.00) {
       costPerMinute = 0.40;
     } else if (isWeekday || isWeekend) { 
    }

    Calculate totalCost = costPerMinute * callLength
    
    return 0;
}
