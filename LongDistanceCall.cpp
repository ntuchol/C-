#include <iostream>
#include <string>
#include <iomanip> // For formatting output

int main() {
    // Declare variables for day, time, length, and cost
    // Get input from the user for day, time, and length
    // Implement input validation
    
    // Determine costPerMinute based on day and time using if-else if-else
    // Example:
    // if (isWeekday && startTime >= 8.00 && startTime <= 18.00) {
    //     costPerMinute = 0.40;
    // } else if (isWeekday || isWeekend) { // handle off-peak and weekends
    //     // assign appropriate rates
    // }

    // Calculate totalCost = costPerMinute * callLength
    // Display totalCost with appropriate formatting
    
    return 0;
}