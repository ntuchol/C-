#include <iostream>
#include <string>
#include <vector> // Or a custom linked list/tree implementation

class Contact {
public:
    std::string name;
    std::string phoneNumber;
    std::string address; // Optional

    // Constructor, getters, setters
};

class TelephoneDirectory {
public:
    // Functions for add, display, search, edit, delete contacts
    // Functions for saving and loading data to/from file
};

int main() {
    // Create TelephoneDirectory object
    // Load existing contacts from file
    // Display menu and handle user input (add, search, etc.)
    // Save contacts to file before exiting
    return 0;
}