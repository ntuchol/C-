// Example of an adjacency list for n vertices
        std::vector<std::list<int>> adj;
        // In the constructor:
        // adj.resize(n); 
        
   // Example of an adjacency matrix for n vertices
        std::vector<std::vector<bool>> adj_matrix;
        // In the constructor:
        // adj_matrix.resize(n, std::vector<bool>(n, false));